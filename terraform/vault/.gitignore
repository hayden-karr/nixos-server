# OpenTofu/Terraform gitignore

# .terraform/ - Provider binaries and cached modules
# Downloaded during 'tofu init', platform-specific, should be re-downloaded per machine
**/.terraform/*

# *.tfstate - STATE FILES CONTAIN ALL SECRETS IN PLAINTEXT JSON
# This is the most critical file to exclude - it has your passwords, tokens, everything
# Never commit this or you expose all secrets
*.tfstate
*.tfstate.*

# crash.log - Debug logs that may contain sensitive output
crash.log
crash.*.log

# *.tfvars - Variable files often contain API keys, passwords, tokens
# Example: resend_api_key = "re_xxxxxxxxxxxx"
*.tfvars
*.tfvars.json

# override.tf - Local-only configuration overrides
# Used for developer-specific settings that shouldn't be shared
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI configuration - User-specific settings
.terraformrc
terraform.rc

# .terraform.lock.hcl - Provider version lock file
# COMMIT THIS - Ensures reproducible builds across machines
# Locks provider versions for consistency
